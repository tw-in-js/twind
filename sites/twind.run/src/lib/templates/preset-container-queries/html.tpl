<main class="@container p-8 space-y-4 text-center">
  <p class="@xs:underline">
    This text will be underlined when the container is larger than `20rem`
  </p>
  <p class="@sm:underline">
    This text will be underlined when the container is larger than `24rem`
  </p>
  <p class="@md:underline">
    This text will be underlined when the container is larger than `28rem`
  </p>
  <p class="@lg:underline">
    This text will be underlined when the container is larger than `32rem`
  </p>
  <p class="@xl:underline">
    This text will be underlined when the container is larger than `36rem`
  </p>
  <p class="@2xl:underline">
    This text will be underlined when the container is larger than `42rem`
  </p>
  <p class="@3xl:underline">
    This text will be underlined when the container is larger than `48rem`
  </p>
  <p class="@4xl:underline">
    This text will be underlined when the container is larger than `56rem`
  </p>
  <p class="@5xl:underline">
    This text will be underlined when the container is larger than `64rem`
  </p>
  <p class="@6xl:underline">
    This text will be underlined when the container is larger than `72rem`
  </p>
  <p class="@7xl:underline">
    This text will be underlined when the container is larger than `80rem`
  </p>
</main>
